"""JobBox — a free, private, personal job-alert aggregator.

Reads job-alert emails from Gmail (read-only), aggregates and de-duplicates
job postings across ALL Gmail tabs, auto-detects application statuses
(Applied / Interview / Rejected), and serves a JobBox-style web dashboard.
"""

__version__ = "0.1.0"
